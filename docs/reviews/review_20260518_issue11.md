# コードレビューレポート

**レビュー日時**: 2026-05-18
**対象**: issue #11 — PassionInsufficientError・拒否メッセージの文言実装
- `hooks/main.go`

**レビュアー**: Claude Code (code-review skill)

---

## 📊 サマリー

| 重要度 | 件数 |
|--------|------|
| 🔴 Critical   | 0 |
| 🟠 Major      | 0 |
| 🟡 Minor      | 1 |
| 🟢 Good       | 4 |
| 🔵 Suggestion | 1 |

**総合評価**: Good

---

## 🔴 Critical Issues

なし。

---

## 🟠 Major Issues

なし。

---

## 🟡 Minor Issues

### [mn-1] `rand.Perm` がGo 1.20以降は自動シードされるが、それ以前は固定シードになる

**ファイル**: `hooks/main.go:91`
**問題**: Go 1.20 未満では `math/rand` のグローバル関数はシード固定（1固定）のため、毎回同じ順番になる。`hooks/go.mod` は `go 1.25.6` なので現状は問題ないが、認識として記録。

```go
// 現在のコード（Go 1.20+ では自動シードされる）
tips := rand.Perm(len(heartRateTips))[:4]
```

Go 1.25.6 を使っているため実害なし。このままで問題ない。

---

## 🟢 Good Points

### [G-1] `rand.Perm` でシャッフルして先頭4件取るのが簡潔
**ファイル**: `hooks/main.go:91`
重複なしでランダム選択する実装として `rand.Perm` は正しい選択。スライスのコピーやマップを使わずシンプル。

### [G-2] issue #11 の出力仕様を完全に満たしている
**ファイル**: `hooks/main.go:84-95`
`Heart rate`・`Threshold`・`Reason`・心拍を上げる方法リストがすべて揃っており、仕様通り。

### [G-3] すべての出力が赤色（`colorRed`）で統一されている
**ファイル**: `hooks/main.go:84`
拒否ブロック全体を `colorRed` で囲み、`colorReset` で終端している。ANSI カラーの開け閉めが正しい。

### [G-4] `heartRateTips` をパッケージレベル変数にしてリストを分離している
**ファイル**: `hooks/main.go:66`
ネタの追加・削除が `printRejected` 関数を触らずにできる構造になっている。

---

## 🔵 Suggestions

### [S-1] 表示件数を定数にする

```go
// 💡 マジックナンバー 4 を定数化すると変更しやすい
const tipCount = 4

tips := rand.Perm(len(heartRateTips))[:tipCount]
```

現状の `:4` でも問題ないが、件数を変えたくなったときに一箇所で済む。

---

## 📝 Test Coverage

| ファイル | 状態 |
|---------|------|
| `hooks/main.go` | ❌ テストなし |

`printRejected` はターミナル出力のため自動テストは難しい。`rand.Perm` の長さチェック（`len(heartRateTips) >= 4`）を起動時に assert するくらいが現実的。ハッカソンスコープでは不要。

---

## ✅ DDD 規約チェック結果

| チェック項目 | 結果 |
|------------|------|
| os.UserHomeDir() 使用 | ✅ パスを扱うコードなし |
| filepath.Join() 使用 | ✅ パスを扱うコードなし |
| log.Fatal は main のみ | ✅（log.Fatal 未使用・os.Exit で制御）|
| エラーを返している | ✅ |
| CGO なし（GOOS=windows OK） | ✅ |
| daemon 未起動時に exit 0 | ✅ |
| タイムアウト時に exit 0 | ✅ |
| stale 時に exit 0 | ✅ |
| 拒否時に赤色（\033[31m） | ✅ |

---

## アクションアイテム

- [ ] [S-1] 表示件数を定数化 — 任意

---

*Generated by Claude Code / code-review skill*
