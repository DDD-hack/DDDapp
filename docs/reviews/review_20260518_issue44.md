# コードレビューレポート

**レビュー日時**: 2026-05-18
**対象**: issue #44 — git commit メッセージへの心拍 trailer 埋め込み + ECG アニメーション
- `hooks/main.go`
- `hooks/prepare-commit-msg`
- `tasks/hooks.toml`

**レビュアー**: Claude Code (code-review skill)

---

## 📊 サマリー

| 重要度 | 件数 |
|--------|------|
| 🔴 Critical   | 0 |
| 🟠 Major      | 0 |
| 🟡 Minor      | 2 |
| 🟢 Good       | 5 |
| 🔵 Suggestion | 2 |

**総合評価**: Good

---

## 🔴 Critical Issues

なし。

---

## 🟠 Major Issues

なし。

---

## 🟡 Minor Issues

### [mn-1] `printECG` の `delay` 計算で width=0 になると除算ゼロになる

**ファイル**: `hooks/main.go:235`
**問題**: `termWidth()` が 0 を返した場合（理論上は返さないが）に `1200/width` がゼロ除算になる。

```go
// 現在のコード
delay := time.Duration(1200/width) * time.Millisecond
```

```go
// ✅ 修正案
if width == 0 {
    width = 80
}
delay := time.Duration(1200/width) * time.Millisecond
```

`termWidth()` がデフォルト80を返すため実害はなし。認識として記録。

---

### [mn-2] `storeBPMForTrailer` の `os.WriteFile` エラーを無視している

**ファイル**: `hooks/main.go:266`
**問題**: BPMファイルの書き込みに失敗してもサイレントに通過する。trailerが付かなくてもcommitは通るので実害は小さいが、デバッグがしにくい。

```go
// 現在のコード
os.WriteFile(bpmFile, []byte(strconv.Itoa(bpm)), 0o644)
```

```go
// ✅ 修正案
if err := os.WriteFile(bpmFile, []byte(strconv.Itoa(bpm)), 0o644); err != nil {
    // trailerが付かないだけでcommitは通る
    fmt.Fprintf(os.Stderr, "⚠ failed to store BPM for trailer: %v\n", err)
}
```

---

## 🟢 Good Points

### [G-1] `prepare-commit-msg` が BPMファイルを使用後に削除している
**ファイル**: `hooks/prepare-commit-msg:7`
`rm -f "$BPM_FILE"` で確実に一時ファイルを削除している。commitsが連続した場合に前回のBPMが混入しない。

### [G-2] `ddd-hook check` サブコマンドが既存の `fetchHeartRate` を再利用している
**ファイル**: `hooks/main.go:83`
新しいHTTPクライアントを作らず既存の関数を使っている。コードの重複がない。

### [G-3] BPMに応じて ECG スパイクの密度が変わる設計
**ファイル**: `hooks/main.go:200`
`period = width * 40 / bpm` で高BPMほどスパイクが密になる。実際の心電図の挙動と一致している。

### [G-4] `hooks:install` で両フックを一括インストールできる
**ファイル**: `tasks/hooks.toml`
`pre-commit` と `prepare-commit-msg` を1コマンドで設置・削除できる。チームメンバーの手順が簡単。

### [G-5] ECG アニメーションが実際の commit で動作確認済み
`git log -1` で `DDDD-BPM: 142` trailer が付いていることを実機確認。issue #44 の完了条件を満たしている。

---

## 🔵 Suggestions

### [S-1] `buildECGRows` の spike 文字列を定数化する

```go
// 現在: 関数内ローカル変数
r0spike := "   /\\   "

// ✅ const にすると意図が明確
const (
    ecgSpikeTop  = "   /\\   "
    ecgSpikeHigh = "  /  \\  "
    ecgSpikeLow  = " /    \\ "
    ecgSpikeBase = "/      \\"
    ecgSpikeW    = 8
)
```

### [S-2] `mise run heartrate` のバイナリパスを相対パスにしている点に注意

**ファイル**: `tasks/hooks.toml:17`
```toml
run = "bin/ddd-hook check"
```
リポジトリルート以外から `mise run heartrate` を実行すると `bin/ddd-hook` が見つからない。`mise run` はリポジトリルートから実行されるので通常は問題ないが、認識として記録。

---

## 📝 Test Coverage

| ファイル | 状態 |
|---------|------|
| `hooks/main.go (buildECGRows)` | ❌ テストなし |
| `hooks/main.go (storeBPMForTrailer)` | ❌ テストなし |
| `hooks/prepare-commit-msg` | ✅ 実機確認済み |

---

## ✅ DDD 規約チェック結果

| チェック項目 | 結果 |
|------------|------|
| os.UserHomeDir() 使用 | ✅ パスを扱うコードなし（git-dir経由） |
| filepath.Join() 使用 | ✅（storeBPMForTrailer） |
| log.Fatal は main のみ | ✅ |
| エラーを返している | ✅（recordCommit） |
| CGO なし（GOOS=windows OK） | ✅ |
| daemon 未起動時に exit 0 | ✅ |
| ECG は純粋 ASCII | ✅（Windows でも文字化けなし） |

---

## アクションアイテム

- [ ] [mn-2] `storeBPMForTrailer` のエラーを stderr に出力 — 推奨・1行
- [ ] [S-1] ECG spike 文字列を定数化 — 任意

---

*Generated by Claude Code / code-review skill*
