# コードレビューレポート

**レビュー日時**: 2026-05-18
**対象**: issue #3 — SQLite 保存
- `daemon/internal/store/store.go`
- `daemon/internal/api/handler.go`
- `daemon/cmd/ddd/main.go`

**レビュアー**: Claude Code (code-review skill)

---

## 📊 サマリー

| 重要度 | 件数 |
|--------|------|
| 🔴 Critical   | 0 |
| 🟠 Major      | 0 |
| 🟡 Minor      | 2 |
| 🟢 Good       | 5 |
| 🔵 Suggestion | 1 |

**総合評価**: Excellent

---

## 🔴 Critical Issues

なし。

---

## 🟠 Major Issues

### [M-1] ~~SQLite の同時アクセスで "database is locked" が起きる可能性~~ ✅ 対応済み

**ファイル**: `daemon/internal/store/store.go:52`
レビュー後に `db.SetMaxOpenConns(1)` を追加済み。並行書き込みによるロック競合は解消されている。

---

## 🟡 Minor Issues

### [mn-1] `SaveSample` の DB エラーが警告ログで握りつぶされている

**ファイル**: `daemon/internal/api/handler.go:64`
**問題**: DB への保存失敗はログに出るが処理を続行する。ハッカソンでは許容範囲だが、保存失敗に気づきにくい。

```go
// 現在のコード（エラーをログだけで流す）
if err := h.db.SaveSample(payload.BPM, "apple_watch"); err != nil {
    c.Logger().Warnf("ws: save sample: %v", err)
}
```

メモリバッファへの `buf.Add` は成功しているので commit 機能は壊れないため、現状のまま（Warn レベル）で問題なし。認識として記録。

---

### [mn-2] `store` パッケージにテストがない

**ファイル**: `daemon/internal/store/`
「DB が開けるか」「テーブルが作成されるか」「SaveSample が正しく insert されるか」を確認するテストがない。インメモリ SQLite（`:memory:`）を使えばファイルを作らずテストできる。

---

## 🟢 Good Points

### [G-1] `os.UserHomeDir()` を正しく使っている
**ファイル**: `daemon/internal/store/store.go:36`
`~` を使わず `os.UserHomeDir()` でホームディレクトリを解決している。Windows でも正しいパスになる。

### [G-2] `filepath.Join()` でパスを結合している
**ファイル**: `daemon/internal/store/store.go:41,46`
`/` のハードコードを避け `filepath.Join()` を使っている。Windows の `\` にも対応できる。

### [G-3] `MkdirAll` でディレクトリを自動作成している
**ファイル**: `daemon/internal/store/store.go:42`
`~/.ddd/` が存在しない初回起動でもエラーにならない。

### [G-4] `CREATE TABLE IF NOT EXISTS` で冪等な初期化
**ファイル**: `daemon/internal/store/store.go:14`
何度起動してもテーブルが重複しない。

### [G-5] `main.go` で `defer db.Close()` が正しく呼ばれている
**ファイル**: `daemon/cmd/ddd/main.go:26`
サーバー終了時に DB が確実にクローズされる。

---

## 🔵 Suggestions

### [S-1] `store` のテストをインメモリ SQLite で書く

```go
// 💡 ファイルを作らずテストできる
func openTestStore(t *testing.T) *Store {
    t.Helper()
    db, err := sql.Open("sqlite", ":memory:")
    if err != nil {
        t.Fatal(err)
    }
    db.SetMaxOpenConns(1)
    if _, err := db.Exec(schema); err != nil {
        t.Fatal(err)
    }
    return &Store{db: db}
}
```

---

## 📝 Test Coverage

| ファイル | 状態 |
|---------|------|
| `store/store.go` | ❌ テストなし |
| `api/handler.go` | ❌ テストなし |
| `hrm/hrm.go` | ✅ 9件 |

---

## ✅ DDD 規約チェック結果

| チェック項目 | 結果 |
|------------|------|
| os.UserHomeDir() 使用 | ✅ |
| filepath.Join() 使用 | ✅ |
| log.Fatal は main のみ | ✅ |
| エラーを返している | ✅ |
| sync.Mutex あり | ✅（hrm）|
| modernc.org/sqlite 使用 | ✅ |
| CGO なし（GOOS=windows OK） | ✅ |
| WebSocket の defer conn.Close() | ✅ |

---

## アクションアイテム

- [x] [M-1] `db.SetMaxOpenConns(1)` を追加 — **対応済み**
- [ ] [mn-2] store のテストを書く — 任意

---

*Generated by Claude Code / code-review skill*
